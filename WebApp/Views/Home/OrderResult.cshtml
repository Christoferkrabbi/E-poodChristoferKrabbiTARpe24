<div>
    <h2>Order Result</h2>

    @if (Model.IsSuccess)
    {
        <div class="success">@Model.Result</div>
    }
    else
    {
        <div class="error">@Model.Result</div>
    }
    <br />
    <a href="/Store/Index">Back to store</a>
</div>